---
scope: Reserving Copilot full system (initiative/feature altitude): reserving_engine, engine_service+copilot_agent, Convex backend, Next.js frontend
purpose: build-substrate
altitude: feature
updated: 2026-07-16T12:19
---

- (decision) Fast path, user pre-decided load-bearing calls in invocation (runtimes, layering, auth, guardrail, audit, testing); architect's job = bind + verify + flag disagreements. PRD prd-agentic-reserving-2026-07-16 + brief addendum + UX spines are sources.
- (decision) Paradigm: functional core / imperative shell on the Python side inside an overall layered three-plane system (TS product plane, Python compute+agent plane, model plane) — ratifies brief addendum's three-plane constraint.
- (constraint) Inherited from PRD/brief (binding, not re-derived): LLM never computes (PRD §5 constitution), Convex sole system of record incl. audit log (addendum ADR), job-record-first orchestration + idempotency by job ID, build sequencing engine-first, Workspace=Clerk org tenancy, two roles analyst/senior-actuary.
- (version) Web-verified 2026-07-16: chainladder 0.9.2 (2026-05-11), anthropic 0.116.0 (2026-07-02), fastapi 0.139.0 (2026-07-01), Next.js 16.2.10 stable, @convex-dev/workflow 0.3.10, convex-test current+documented, Clerk<->Convex JWT template still the documented integration path.
- (decision) CONFLICT surfaced + adopted: user invocation specifies Anthropic Python SDK for copilot_agent; finalized brief/addendum committed Gemini gemini-3.1-flash-lite + Agno (OQ-1 in PRD). Treating the user's CA invocation as Rohan (product owner) resolving OQ-1 in favor of Anthropic. Consequence: Agno's provider-agnosticism is given up; mitigation AD keeps tool schemas + prompts provider-neutral so a swap stays a contained change. Brief addendum should be updated to match — flagged to user.
- (decision) Divergence from user's layout (flagged): diagnostics COMPUTATION lives in reserving_engine as pure functions (numbers must originate in the golden-tested core); engine_service's diagnostics module is only the serialisation/HTTP view. User said 'engine_service ... and a diagnostics module' — interpreted as the exposure point, not the computation home.
- (decision) Provenance Gate = two mechanisms: (1) template injection — agent drafts carry placeholders {{dx:...}} / {{rs:...}} resolved from ResultSet/DiagnosticsBundle at render, agent never emits final figures; (2) numeric-token checker as second line — post-render draft scanned, every numeric token must match a source value under a canonicalization rule (documented rounding/formatting, whitelist for structural numerals like section numbers). Gate runs in engine_service before any draft is returned; rejected drafts never persisted as reviewable, rejection logged.
- (decision) Audit hash chain: per-workspace chain, entry hash = sha256(canonical JSON of entry + prevHash); Convex OCC serializes inserts so chain-head contention resolves by retry; append via a single internal mutation appendAuditEntry — no other code path writes auditLogs; verification routine walks chain.
- (decision) Reproducibility (PRD FR-6 assumption resolved): golden tests assert exact equality on the pinned platform (linux/amd64 CI + Cloud Run image); cross-platform re-derivation documented with epsilon 1e-8 relative tolerance. uv lock pins the engine dependency set; Lineage records engine semver + chainladder version + triangle sha256 + params.
- (decision) Orchestration: Convex run record is the job authority (status queued|running|complete|failed). v1 = Convex action awaits engine_service synchronously with timeout; request carries runId as idempotency key; engine_service is stateless so retry-safe; contract shaped so async 202+HMAC callback is additive (addendum headroom). @convex-dev/workflow 0.3.10 for durable execution + retries.
- (decision) Auth: Clerk JWT template 'convex'; Convex auth.config.ts binds issuer; every public Convex function calls requireMember(ctx, workspaceId) (identity + org membership) and role-gated ones call requireRole(ctx, ws, 'senior_actuary'); roles carried as Clerk org roles in JWT claims. Engine_service trusts only Convex: shared-secret (bearer) service auth on all engine endpoints; browser never calls engine_service.
- (decision) Diagnostic ID format fixed (cross-runtime shared contract): dx:{runId}:{kind}:{key} where kind in {ldf_stability, ave, cl_bf_divergence, residual}; key is origin/dev-period coordinate; generated only by reserving_engine; resolvable by Convex query and by agent read tool.
- (decision) Deployment envelope: engine_service (engine+agent) one container on Cloud Run (addendum); Convex cloud (prod+dev deployments); Next.js on Vercel; secrets: ANTHROPIC_API_KEY + service secret live only in Cloud Run env, Clerk keys in Vercel+Convex env. Model ID + token/cost ceiling per Run are engine_service config values.
- (assumption) File storage: uploaded CSV/xlsx originals stored in Convex file storage, parsed canonical triangle JSON stored in triangles table with sha256 of canonical content as the Triangle hash. Not user-specified; tagged.
- (question) OQ carried: incurred-triangle validation rules need actuarial confirmation before ingestion epic (PRD OQ-6); per-Run token/cost ceiling values to be set at agent-layer eval (PRD §8).
- (event) Reviewer Gate run sequentially (no subagents per session policy): lint (2 false-positive placeholder hits on the intentional dx ID format spec, verified), web-verification lens PASS, adversarial lens found 3 holes (triangle hash basis, gate scope over human edits, transcript durability under sync crash) — first two fixed in spine, third recorded in Deferred with async-upgrade trigger; rubric walker PASS, observability added to Deferred. Reviews in reviews/.
- (decision) NFR-7 budgets set: engine run <=60s p95 end-to-end for <=30 origin periods; interpretation <=10min hard-bounded by per-Run ceiling; breach fails cleanly, never silent queueing.
- (event) spine finalized
