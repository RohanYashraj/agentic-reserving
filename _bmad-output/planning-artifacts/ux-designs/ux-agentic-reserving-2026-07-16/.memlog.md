---
topic: Reserving Copilot UX
updated: 2026-07-16T12:07
---

- (decision) Fast path, non-interactive: sources = finalized PRD + addendum (prd-agentic-reserving-2026-07-16); scope per user = upload-to-report journey, diagnostics review screen, senior-actuary approve/publish flow. Full IA sketched for coherence; depth only on the three scoped areas.
- (assumption) UI system = shadcn/ui on Next.js + Tailwind (natural fit for committed Next.js App Router stack; not user-confirmed). DESIGN.md is brand-layer delta only.
- (assumption) Brand direction not elicited (no interactive user): restrained 'instrument-grade' posture chosen — slate/ink neutrals, deep teal primary, dedicated provenance violet used ONLY for citation chips/diagnostic links, amber reserved for Engine-Only Mode and validation warnings. Tagged for user override.
- (decision) Citation chip is the signature component: every Interpretation claim renders one; clicking navigates to the Diagnostic (PRD FR-8). Provenance color never used decoratively.
- (decision) Key-screen mocks: diagnostics-review.html and report-review.html (layout drives behavior). Upload wizard, run config, audit log = spine-only (standard patterns).
- (event) Key-screen mocks rendered and verified in browser: mockups/diagnostics-review.html, mockups/report-review.html. One self-caught DESIGN violation fixed (divergence bars were violet; violet is provenance-only — now teal with violet selection outline).
- (decision) Mock coverage: Diagnostics review + Report review mocked; Dashboard, Upload wizard, Run config, Audit Log, Settings are spine-only (standard shadcn patterns; layout does not drive novel behavior).
- (event) Reviewer Gate skipped (opt-in; non-interactive run, no user to select lenses). Available on demand via bmad-ux Validate intent.
- (decision) Open items (all non-blocking, need Rohan's confirmation): (1) shadcn/ui + entire visual direction chosen without elicitation; (2) no token-streaming for Interpretation output — gated-complete display only; (3) draft locks read-only on submit-for-review; (4) mobile approve/publish in scope for Senior Actuary. All tagged [ASSUMPTION] in spines.
- (event) spines finalized
